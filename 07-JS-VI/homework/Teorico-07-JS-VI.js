Callback: sirve para pasar una funcion como argumento a otra funcion. 

.forEach: toma un callback como su unico argumento, e itera sobre cada elemento de la matriz y llama al CB en él. 

.reduce: ejecuta un bucle en la matriz con la intencion de reducir cada elemento en un elemento que se devuelve. 

.map: se usa para cambiar cada elemento de una matriz de la misma manera. 